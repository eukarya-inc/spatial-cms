CMS_URL=http://localhost:3001/api/v1
CMS_API_KEY=scms_your_key_here
# Workspace the CMS_API_KEY is bound to. API keys are strictly workspace-scoped;
# this must match or every request returns 403. Defaults to "default".
CMS_WORKSPACE=default
PORT=8090
