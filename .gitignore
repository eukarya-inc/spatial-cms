node_modules/
dist/
.env
*.db
*.db-journal
.dev-logs/

# Large input data for seed scripts (CityGML, GeoJSON dumps, etc.).
# Drop files here locally; they're too big and license-encumbered for the repo.
scripts/data/

# SSH keys (never commit)
*.pem
deploy/.keycloak_admin_password.txt
deploy/.deployment_secrets.txt

# Local plan + memory files (per-machine, not for the repo)
.claude/
# Per-machine Claude Code working notes — not project documentation.
CLAUDE.md
